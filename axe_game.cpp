#include "raylib.h"



int main(){
    
    /*
        You need to put the loop AFTER InitWindow to see the popup window
    */

    int h = 750;
    int w = 900;

    InitWindow(h, w, "POPUP");
    
    while (true)
    {
        /* code */
    }
}